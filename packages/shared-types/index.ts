// Types shared between apps/api and apps/web (enums, DTO shapes) live here so
// the two apps can never drift apart on a contract. Starting in Phase 1 with
// auth/role types; grows as each phase adds endpoints.

export {}
